class Solution:
    def missingNumber(self, nums: List[int]) -> int:
        nums.sort()
        curr = 0
        for i in range(len(nums)):
            if nums[i] == curr:
                curr+=1
            else:
                return curr
        return curr 
