from collections import defaultdict

class Solution:
    def majorityElement(self, nums: List[int]) -> List[int]:
        counter = defaultdict(int)
        for elem in nums:
            counter[elem] += 1
        return [elem for elem, cnt in counter.items() if cnt > len(nums) / 3]
